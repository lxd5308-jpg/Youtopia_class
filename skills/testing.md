# Testing

Skills to use during testing and validation:

- Manual feature testing for teacher and student workflows
- Integration testing across `App.jsx` data flow and page components
- Regression testing after changes to preserve existing behavior
- Verifying login/auth, schedule, messages, payments, and attendance paths
- Checking that real-time Firestore updates reflect correctly in the UI
- Ensuring the app works on web and mobile breakpoints
- Reviewing built output in the latest `dist` folder before deployment
